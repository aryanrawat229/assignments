//You are provided a four digit number N only. Your task is to print out the first digit of that number.

let N = 1234;

function Check(N) {
    console.log(N[0]);
}

Check(N.toString()); //converting number to string to access the first digit
